import { BoolenFilterPipe } from './boolen-filter.pipe';

describe('BoolenFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BoolenFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
